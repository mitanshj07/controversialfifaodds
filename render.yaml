services:
  - type: web
    name: controversialfifaodds
    runtime: node
    repo: https://github.com/mitanshj07/controversialfifaodds
    branch: main
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
